Remove Google sign-in from `src/pages/Auth.tsx`:

1. Delete the `handleGoogle` function.
2. Delete the "or" divider block.
3. Delete the "Continue with Google" button.

Keep email/password sign in, sign up (with display name), forgot password flow, mode toggle, and toast states untouched. No other files modified.